/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/high-look-steel-furniture",
  assetPrefix: "/high-look-steel-furniture/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;