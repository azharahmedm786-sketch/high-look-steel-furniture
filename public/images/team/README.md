# Image folder

Drop real business photographs here.

The site currently shows a labeled placeholder wherever an image from
this folder is referenced (see components/ui/PlaceholderImage.tsx).
Once a real file exists at the expected path, follow the swap
instructions at the top of that component to switch it over to
next/image.
